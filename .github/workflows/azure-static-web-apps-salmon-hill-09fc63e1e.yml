
ChatGPT:
yaml
コピーする
編集する
name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      # 1. ソースコードをチェックアウト
      - uses: actions/checkout@v3
        with:
          submodules: true
          lfs: false

      # 2. Python をセットアップ（Functions をビルド可能にする）
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      # 3. Azure Functions の依存パッケージをインストール
      - name: Install function dependencies
        working-directory: api
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      # 4. Static Web Apps へデプロイ
      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_SALMON_HILL_09FC63E1E }}
          repo_token:                    ${{ secrets.GITHUB_TOKEN }}
          action:                        "upload"
          app_location:                  "/"      # index.html のあるルート
          api_location:                  "api"    # Azure Functions コードがあるフォルダ
          output_location:               ""       # 静的コンテンツのビルド出力先（ルート直下）

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: Close Pull Request
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_SALMON_HILL_09FC63E1E }}
          action: "close"
